version: "3.4"
services:
    tedicross:
        image: tsunderechen/tedicross-docker
        deploy:
            replicas: 1
            restart_policy:
                condition: on-failure
        working_dir: /app
        command: "npm start"
        secrets:
            - tedicross-settings.yaml
secrets:
    tedicross-settings.yaml:
        external: true
